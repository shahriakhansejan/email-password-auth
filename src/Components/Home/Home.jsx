import React from 'react';
import PropTypes from 'prop-types';

const Home = props => {
    return (
        <div>
            <h2>This is home</h2>
        </div>
    );
};

Home.propTypes = {
    
};

export default Home;